{
  "name": "proxy-server",
  "description": "A multi-protocol proxy server with SOCKS5, HTTP, and TLS support, featuring rule-based routing and upstream proxy chaining",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "start": "node ./dist/index.js"
  },
  "dependencies": {
    "binary-rw": "github:zuenkoma/binary-rw"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  }
}
